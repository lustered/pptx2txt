## Extract PowerPoints into txt files.

### Usage

    .Place the files in the same directory as 'noter.py'
    .run python3 noter.py
    .be thankful you don't have to manually type notes from powerpoints.
